'use client';import { Check,Field,TextArea } from './fields';import { MediaPicker } from '@/components/admin/media/MediaPicker';import type { MediaAsset } from '@/lib/cms/media'
export function NewsForm({value,onChange,mediaAssets}:{value:any;onChange:(n:string,v:unknown)=>void;mediaAssets?:MediaAsset[]}){return <div className="editor-form"><Field label="Title" name="title" value={value.title} onChange={onChange} required/><Field label="Slug" name="slug" value={value.slug} onChange={onChange}/><TextArea label="Excerpt" name="excerpt" value={value.excerpt} onChange={onChange}/><TextArea label="Body" name="body" value={value.body} onChange={onChange} rows={12}/><Field label="Author" name="author" value={value.author} onChange={onChange}/><div className="form-field-group"><label>Background image</label><p className="hint">Shown as the full-width background of the post header.</p>{mediaAssets?<MediaPicker assets={mediaAssets} value={value.coverMediaId} onChange={id=>onChange('coverMediaId',id)}/>:<p className="hint">Upload an image in the Media Library first, then come back to select it here.</p>}</div><Check label="Feature this post" name="featured" value={value.featured} onChange={onChange}/></div>}
