'use client';import { Check,Field,TextArea } from './fields';import { usePageAddress } from './usePageAddress';import { MediaPicker } from '@/components/admin/media/MediaPicker';import type { MediaAsset } from '@/lib/cms/media'
export function NewsForm({value,onChange,mediaAssets,autoSlug=false}:{value:any;onChange:(n:string,v:unknown)=>void;mediaAssets?:MediaAsset[];autoSlug?:boolean}){const change=usePageAddress(onChange,autoSlug);return <div className="editor-form"><Field label="Title" name="title" value={value.title} onChange={change} required/><Field label="Page address" name="slug" value={value.slug} onChange={change}/><TextArea label="Excerpt" name="excerpt" value={value.excerpt} onChange={onChange}/><TextArea label="Body" name="body" value={value.body} onChange={onChange} rows={12}/><Field label="Author" name="author" value={value.author} onChange={onChange}/><div className="form-field-group"><label>Background image</label><p className="hint">Shown as the full-width background of the post header.</p>{mediaAssets?<MediaPicker assets={mediaAssets} value={value.coverMediaId} onChange={id=>onChange('coverMediaId',id)}/>:<p className="hint">Upload an image in the Media Library first, then come back to select it here.</p>}</div><Check label="Feature this post" name="featured" value={value.featured} onChange={onChange}/></div>}
